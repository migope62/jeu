import React from 'react';

function TuileThermes() {
    return (
        <div
            style={{
                width: '50px', // Largeur de la tuile
                height: '50px', // Hauteur de la tuile
                backgroundColor: '#87CEEB', // Couleur de fond pour les thermes (bleu clair)
                border: '1px solid #4682B4', // Bordure de la tuile (une teinte plus foncée de bleu clair)
                /* Ajoutez d'autres styles pour personnaliser l'apparence de la tuile de thermes */
            }}
        >
            {/* Vous pouvez ajouter des détails supplémentaires ici, comme des portes ou des fenêtres */}
        </div>
    );
}

export default TuileThermes;
