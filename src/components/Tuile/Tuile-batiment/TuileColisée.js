import React from 'react';

function TuileColisee() {
    return (
        <div
            style={{
                width: '50px', // Largeur de la tuile
                height: '50px', // Hauteur de la tuile
                backgroundColor: '#A0522D', // Couleur de fond pour le Colisée (marron clair)
                border: '1px solid #8B4513', // Bordure de la tuile (une teinte plus foncée de marron)
                /* Ajoutez d'autres styles pour personnaliser l'apparence de la tuile du Colisée */
            }}
        >
            {/* Vous pouvez ajouter des détails supplémentaires ici, comme les arcs ou les gradins */}
        </div>
    );
}

export default TuileColisee;
